# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- ローカル実行の出力 (make run-local) ---
.local/

# --- Terraform ---
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# .tfvars には値が入るのでコミットしない (.tfvars.example は追跡する)
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl は意図的に追跡する (プロバイダのバージョン固定)

# --- シークレット ---
.env
.env.*
!.env.example

# --- OS / エディタ ---
.DS_Store
.idea/
*.swp
