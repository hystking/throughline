# CLAUDE.md

## README にコードを読めば分かることを書かない

コマンド一覧・オプション・出力先といった、`Makefile` やスクリプトを読めば
分かることは README に書かない。二重管理になり、必ず片方が古くなる。

README は「何を作るのか」への入口だけ。手順はそれを実行するファイル
(`Makefile`, `scripts/*`) 側に、コメントとして置く。
